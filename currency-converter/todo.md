# TODO

- Add unit tests for:
  - ConversionService
  - Forced rate deviation logic
- Extract currency switch into its own component
- Add debounce on input to reduce recalculations
- Persist history in localStorage
